{
  "name": "proto-photon",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "ASTRO_CONFIG_DIR='./.astro-telemetry' astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^6.4.7"
  }
}
